# Postgres connection used by the web app
DATABASE_URL=postgresql://balanceproof:balanceproof@localhost:5432/balanceproof
# Parsing worker base URL
WORKER_URL=http://localhost:8000
# Where uploaded PDFs are written (shared between web + worker)
STORAGE_DIR=./data/uploads
